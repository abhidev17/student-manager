{
  "name": "frontend-wrapper",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix output install",
    "dev": "npm --prefix output run dev -- --host 127.0.0.1 --strictPort --port 5173",
    "build": "npm --prefix output run build",
    "preview": "npm --prefix output run preview",
    "lint": "npm --prefix output run lint"
  }
}
